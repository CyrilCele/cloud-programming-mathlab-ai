name: Terraform Apply

on:
  workflow_dispatch:

jobs:
  terraform-apply:
    name: Terraform Apply
    runs-on: ubuntu-latest

    steps:
      - name: Information
        run: |
          echo "Terraform Apply will be enabled after"
          echo "AWS authentication is configured"
          echo "and deployment approvals are implemented."
