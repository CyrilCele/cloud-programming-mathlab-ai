#!/usr/bin/env bash

set -euo pipefail

source "$(dirname "${BASH_SOURCE[0]}")/../common/paths.sh"
source "$(dirname "${BASH_SOURCE[0]}")/../common/logging.sh"
source "$(dirname "${BASH_SOURCE[0]}")/../common/assertions.sh"
source "$(dirname "${BASH_SOURCE[0]}")/../common/utils.sh"